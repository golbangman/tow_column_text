# Product

## Definition

YouTube 영어 영상의 링크를 넣으면 영어 자막을 받아 문장 단위로 한국어로 번역하고, 같은 화면에서 영상 재생 화면과 좌측 영어·우측 한국어 대역 문서를 함께 보여주며, 인쇄해서 필기하며 대조하는 개인용 웹 도구다. 사용자는 도구를 만든 본인 한 명이며, 영상을 재생하면서 대역 문서를 스스로 스크롤해 따라 읽고, 나중에 인쇄해서 원문과 번역을 문장 단위로 대조한다.

## Users and situations

사용자는 이 도구를 만든 당사자 한 명이다. 유튜브 기술 영상을 정확히 이해하려는 상황에서 쓴다. 영상을 재생하면서 대역 번역을 마우스 휠로 스스로 스크롤해 재생 위치를 따라 읽고, 나중에 인쇄해서 원문과 번역을 문장 단위로 대조하며 공부한다.

## Problem and current alternatives

지금은 다른 도구(`youtube_digger`)로 자막·번역 텍스트 파일을 먼저 만들어 이 앱에 업로드해야 한다. 파일을 준비하는 단계가 번거롭고, 영상을 보면서 번역을 참고할 수 없다.

## Promised change

링크 하나만 넣으면 자막 추출과 문장별 번역까지 앱이 처리하고, 영상을 재생하며 같은 화면의 대역 문서를 따라 읽을 수 있다. 별도 파일을 준비하는 단계가 사라진다.

## Core loop

1. 홈에서 URL 입력부 아래 저장된 영상 목록을 본다.
2. YouTube 영상 링크를 입력한다.
3. 앱이 그 영상의 영어 자막을 받아(자동 생성 자막 포함) 문장 단위로 쪼갠다. 영어 자막이 없으면 안내하고 멈춘다.
4. 문장을 순서대로 하나씩 번역해 우측을 채운다. 진행률이 보이고 중간에 멈출 수 있다. 실패한 문장은 영어만 남고, 그 행의 "번역" 버튼으로 하나씩 다시 시도한다.
5. 한쪽에 영상 재생 화면과 조작, 다른 쪽에 좌측 영어·우측 한국어 대역 문서가 있다. 대역 문서 영역은 고정돼 있고 마우스 휠로 문장을 위아래로 스크롤한다. 사용자가 재생 위치에 맞춰 스스로 따라간다. 재생과 자막을 시간으로 동기화하거나 현재 문장을 강조하지 않는다.
6. 결과를 저장한다. 저장된 문서를 다시 열면 재번역 없이 바로 보이고, 번역 안 된 문장이 남으면 이어서 번역한다.
7. 번역 문서를 브라우저 인쇄 대화상자로 인쇄하거나 PDF로 저장한다. 인쇄본에는 영상 없이 번역 문서만 나온다.
8. 홈에서 URL 입력부 아래에 저장된 영상 목록을 보여준다. 자막이 생성된 항목에는 자막 아이콘이 붙는다. 항목을 클릭하면 그 영상의 번역 페이지가 저장 상태 그대로 열린다.

## Capabilities and boundaries

- 입력은 YouTube 영상 링크다. 파일 업로드는 받지 않는다.
- 앱이 영상의 영어 자막을 직접 받는다. 자동 생성 자막도 받는다. 영어 자막이 없는 영상은 처리하지 않고 안내한다.
- 번역은 영어에서 한국어로, 문장 단위 1:1이다. 앱이 구글 번역을 통해 수행한다.
- 번역은 순차적이다. 진행률을 표시하고 중간 정지를 지원한다. 실패한 문장은 영어만 남기고, 행별 "번역" 버튼으로 다시 시도한다.
- 화면에 영상 재생 화면과 대역 문서를 함께 보여준다. 대역 문서 영역은 고정되고 마우스 휠로 스크롤한다. 재생과 자막을 시간으로 동기화하거나 현재 문장을 강조하지 않는다.
- 번역 결과와 진행 상태를 보관한다. 재열람 시 재번역하지 않는다. 멈춘 문서는 이어서 번역한다.
- 문서에 편집 기능이 없다. 좌우 모두 읽기 전용이다.
- 출력은 브라우저 인쇄 대화상자로 한다. A4 세로, 매 쪽 머리말에 제목과 링크, 문장 번호와 영어·한국어 2열, 좌우에 필기용 여백이 들어간다. 인쇄본에 영상과 조작 버튼은 나오지 않는다. 자동 쪽번호는 없다.
- 로그인과 계정이 없다. 개인용 도구다.

## Experience principles

- 링크 하나만 넣으면 끝나는 단순함.
- 영상을 보면서 같은 화면에서 대역 번역을 참고하는 몰입감.
- 좌우는 항상 1:1로 맞는다. 어긋난 짝을 보이지 않는다.
- 오래 걸리는 번역이 투명하다. 얼마나 됐고 얼마 남았는지 보이고, 언제든 멈출 수 있다.
- 한 번 한 번역은 버리지 않는다. 저장되고, 멈춘 지점부터 이어진다.
- 인쇄 우선. 화면의 대역 문서가 인쇄될 지면과 비슷하게 보인다.

## Success signals

- 링크만 넣으면 자막과 번역이 나와서 다른 도구를 거치지 않는다.
- 영상을 보며 대역 번역을 스크롤해 따라 읽는 것이 자연스럽다.
- 대역 문서의 행이 어긋나지 않아, 원문과 번역을 문장 단위로 믿고 대조한다.
- 인쇄해서 공부한다.
- 같은 영상을 다시 열 때 기다리지 않는다.

## Assumptions and unknowns

- Assumption: 자동 생성 자막은 구두점이 부족해 문장 분리가 잘 안 될 수 있다. 그러면 큰 덩어리가 통째로 번역돼 좌우 짝이 거칠어진다. 이 품질을 감수한다.
- Assumption: 자막 추출은 `yt-dlp` 같은 외부 도구에 기댄다. 그 도구가 없거나 YouTube가 막으면 자막을 받지 못한다.
- Assumption: 영상 재생은 YouTube 임베드 플레이어를 쓴다. 임베드가 막힌 영상은 재생이 안 될 수 있다.
- Assumption: 구글 번역 웹을 자동화로 순차 호출하면 차단(rate limit)을 겪을 수 있다. 실패한 문장은 영어만 남기고 행별 "번역" 버튼으로 회복한다.
- Unknown: 저장 위치(브라우저 대 백엔드)와 목록의 정렬·탐색 방식.
- Unknown: 번역이 대량으로 실패할 때 사용자에게 알리고 재시도를 권하는 상세.
- Unknown: 영상 영역과 대역 문서 영역의 화면 분할 비율.
- Unknown: 재생 위치에 맞춘 자막 자동 스크롤은 이후에 추가할 수 있다. 지금은
  수동 스크롤만 한다.
