# Product

## Definition

`youtube_digger`에서 나온 영어 자막 텍스트 파일의 영어를 문장 단위로 쪼개 한 문장씩 한국어로 번역하고, 좌측 영어와 우측 한국어를 나란히 놓은 인쇄용 2단 문서를 만들어 보관하는 개인용 웹 도구다. 사용자는 도구를 만든 본인 한 명이며, 문장이 어긋나 믿기 어려웠던 대역 문서를, 행이 항상 맞는 문서로 바꿔 종이에 필기하며 대조한다.

## Users and situations

사용자는 `youtube_digger`를 만든 당사자 한 명이다. 유튜브 기술 영상의 자막을 손에 들고 내용을 정확히 파고들려는 상황에서 이 도구를 찾는다. 화면을 오래 보기 싫고, 펜으로 필기하며 영어 원문과 한국어 번역을 문장 단위로 나란히 두고 대조하고 싶을 때다.

## Problem and current alternatives

지금은 파일에 들어 있는 `=== 한국어 번역 ===` 섹션을 영어와 위치로 짝지어 2단 문서를 만든다. 그 한국어는 영어와 문장 경계가 달라서, 위치로 짝지으면 좌우 행이 어긋난다. 어긋난 대역 문서로는 문장 단위 대조를 할 수 없다. 지금은 어긋난 짝을 감수하거나 원문 파일을 손으로 고쳐 다시 올린다.

## Promised change

영어를 문장 단위로 쪼개 각 문장을 따로 번역하므로 좌우가 항상 1:1로 맞는다. 원문과 번역을 문장 단위로 믿고 대조할 수 있게 된다.

## Core loop

1. 홈에서 저장된 2단 문서 목록을 본다. "문서 변환"으로 진입한다.
2. 영어 자막 텍스트 파일을 올린다. 파일에 한국어 섹션이 있어도 무시한다.
3. 영어를 문장 단위로 쪼개 좌측에 배치한 2단 문서를 먼저 보여준다. 우측 한국어 칸은 빈 상태다.
4. "번역 시작"을 누르면 문장을 순서대로 하나씩 번역해 우측을 채운다. 진행률이 보이고, 중간에 멈출 수 있다.
5. 번역에 실패한 문장은 우측을 비우고 영어만 남긴다.
6. 결과를 저장한다. 저장된 문서를 다시 열면 재번역 없이 바로 보인다. 멈췄던 문서를 다시 열면 번역 안 된 문장부터 이어서 번역한다.
7. 완성된 2단 문서를 브라우저 인쇄 대화상자로 인쇄하거나 PDF로 저장한다.
8. 목록의 항목을 클릭하면 읽기 전용 뷰어가 열린다. 편집 기능은 없다.

## Capabilities and boundaries

- 입력은 `youtube_digger`가 만든 형식의 텍스트 파일이다. 한국어 섹션은 무시하고 영어 본문만 쓴다.
- 번역은 영어에서 한국어로, 문장 단위 1:1이다. 앱이 구글 번역을 통해 수행한다.
- 번역은 순차적이다. 진행률을 표시하고 중간 정지를 지원한다. 실패한 문장은 영어만 남긴다.
- 번역 결과와 진행 상태를 보관한다. 재열람 시 재번역하지 않는다. 멈춘 문서는 이어서 번역한다.
- 문서에 편집 기능이 없다. 좌우 모두 읽기 전용이다.
- 문장 짝짓기는 위치 기반이다. 좌측 문장 i가 우측 번역 i와 짝이 된다.
- 출력은 브라우저 인쇄 대화상자로 한다. A4 세로, 매 쪽 머리말에 제목과 URL, 좌우에 필기용 여백이 들어간다.
- 로그인과 계정이 없다. 개인용 도구다.

## Experience principles

- 좌우는 항상 1:1로 맞는다. 어긋난 짝을 보이지 않는다.
- 오래 걸리는 번역이 투명하다. 얼마나 됐고 얼마 남았는지 보이고, 언제든 멈출 수 있다.
- 한 번 한 번역은 버리지 않는다. 저장되고, 멈춘 지점부터 이어진다.
- 인쇄 우선. 화면의 2단 문서가 인쇄될 지면과 비슷하게 보인다.
- 조작은 적다. 올리기에서 번역을 거쳐 인쇄까지 흐름이 짧다.

## Success signals

- 대역 문서의 행이 어긋나지 않아, 원문과 번역을 문장 단위로 믿고 대조한다.
- 긴 자막도 끝까지 번역돼 인쇄해서 공부한다.
- 같은 문서를 다시 열 때 기다리지 않는다.

## Assumptions and unknowns

- Assumption: 구글 번역 웹을 자동화로 순차 호출하면 차단(rate limit)을 겪을 수 있다. 실패한 문장은 영어만 남기는 것으로 감수하고 "이어서 번역"으로 회복한다. (`youtube_caption`의 follow-up이 이 위험을 실증한다.)
- Assumption: 문장 분리는 종결 부호 기반의 추정이라 완벽하지 않다. 잘못 쪼개진 문장은 그 상태로 번역된다. 편집으로 고칠 수 없다.
- Unknown: 저장 위치(브라우저 대 백엔드)와 목록의 정렬·탐색 방식은 제품 차원에서 정하지 않았다.
- Unknown: 번역 실패가 대량으로 날 때 사용자에게 어떻게 알리고 언제 재시도를 권하는지의 상세.
